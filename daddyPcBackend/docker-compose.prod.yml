services:
  app:
    image: daddy-backend-app:v1.0.0
    build:
      context: .
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DOPPLER_TOKEN: ${DOPPLER_TOKEN}
      SPRING_DOCKER_COMPOSE_ENABLED: "false"
